language: python
python:
    - "2.7"
install: 
    - pip install codecov
    - pip install .
    - pip install -r requirements.txt

script: 
    - python -m unittest testAll
after_success:
    codecov
    